# See AGENTS.md

The operating rules for this repository live in [AGENTS.md](AGENTS.md), not
here. This file exists only because Claude Code auto-loads the filename
`CLAUDE.md` — edit `AGENTS.md` when rules need to change, so every agent
tool reading this repo sees the same rules.
